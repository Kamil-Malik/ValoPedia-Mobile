<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="16dp">

    <ImageView
        android:id="@+id/iv_weapon_skin_icon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="@string/weapons"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/odin" />

    <TextView
        android:id="@+id/tv_weapon_skin_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:fontFamily="@font/sen_bold"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="@id/iv_weapon_skin_icon"
        app:layout_constraintStart_toStartOf="@id/iv_weapon_skin_icon"
        app:layout_constraintTop_toBottomOf="@id/iv_weapon_skin_icon"
        tools:text="Example of weapon skin name" />

</androidx.constraintlayout.widget.ConstraintLayout>